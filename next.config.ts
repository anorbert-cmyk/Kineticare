import { withPayload } from '@payloadcms/next/withPayload'
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // A későbbi sprintek Next.js-konfigurációja ide kerül.
}

export default withPayload(nextConfig)
